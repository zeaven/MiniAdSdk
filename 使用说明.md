# AdSdk 使用说明

## 引用

**复制 core 目录到项目中，与游戏目录冲突可以改名

```js
import AdSdk from './core/AdSdk'
//或
const AdSdk = require('./core/AdSdk')
```

## 初始化

```js
// 初始化
AdSdk.instance.init({debug: true,})
// 需要隐私登录的情况下，先监听隐私弹窗事件，再初始化
AdSdk.instance.init({
   debug: true,
   privacy: (ctx) => {
     // 显示隐私弹窗
     //   privacyDlg.visible = true
     // 如果用户同意隐私，调用 agreePrivacy，会通知Sdk继续初始化
     ctx.agreePrivacy()
     // 隐藏隐私弹窗
     //   privacyDlg.visible = true
   }
})
```

## Banner广告

```js
AdSdk.instance.showBanner().then((result) => {
   // 展示成功回调
}).catch((err) => {
   // 展示错误回调
})
```

## 插屏广告

```js
AdSdk.instance.showInters().then((result) => {
   // 展示成功回调
}).catch((err) => {
   // 展示错误回调
})
```

## 激励视频广告

```js
AdSdk.instance.showReward().then((result) => {
   // 展示成功回调
      result.rewardPromise?.then(() => {
      // 完成奖励回调
      }).catch((err) => {
      // 失败奖励回调
      })
}).catch((err) => {
   // 展示错误回调
})
```

## 原生广告
```js
AdSdk.instance.showNative().then((result) => {
   // 展示成功回调
}).catch((err) => {
   // 展示错误回调
})
```
