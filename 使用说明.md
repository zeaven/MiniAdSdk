# AdSdk 使用说明

## 引用

**复制 core 目录到项目中，与游戏目录冲突可以改名

```js
import AdSdk from './core/AdSdk'
//或
const AdSdk = require('./core/AdSdk')
```

## 初始化

```js
AdSdk.instance.init()
```

## Banner广告

```js
AdSdk.instance.showBanner().then((result) => {
   // 展示成功回调
}).catch((err) => {
   // 展示错误回调
})
```

## 插屏广告

```js
AdSdk.instance.showInterstitial().then((result) => {
   // 展示成功回调
}).catch((err) => {
   // 展示错误回调
})
```

## 激励视频广告

```js
AdSdk.instance.showRewardVideo().then((result) => {
   // 展示成功回调
   if (result.rewardPromise) {
       result.rewardPromise.then(() => {
         // 完成奖励回调
       }).catch((err) => {
         // 失败奖励回调
       })
   }
}).catch((err) => {
   // 展示错误回调
})
```
