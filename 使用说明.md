# AdSdk 使用说明

## 引用

**复制 AdSdk 目录到项目中，AdSdk.core.js是否设置为插件都可使用**

```js
import AdSdk from 'AdSdk/index'
//或
const AdSdk = require('AdSdk/index')
```

## 初始化

```js
AdSdk.instance.init()
```

## Banner广告

```js
AdSdk.instance.showBanner().then((result) => {
   // 展示成功回调
}).catch((err) => {
   // 展示错误回调
})
```

## 插屏广告

```js
AdSdk.instance.showInterstitial().then((result) => {
   // 展示成功回调
}).catch((err) => {
   // 展示错误回调
})
```

## 激励视频广告

```js
AdSdk.instance.showRewardVideo().then((result) => {
   // 展示成功回调
   if (result.rewardPromise) {
       result.rewardPromise.then(() => {
         // 完成奖励回调
       }).catch((err) => {
         // 失败奖励回调
       })
   }
}).catch((err) => {
   // 展示错误回调
})
```
