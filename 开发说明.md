# AdSdk 开发说明

1. [核心目录结构](#核心目录结构)

2. [主要文件](#主要文件)

3. [AdSdk](#AdSdk)

   3-1. [初始化流程](#初始化流程)
   
   3-2. [配置适配器](#配置适配器)
   
   3-3. [适配器的加载](#适配器的加载)

4. [适配器](#适配器)

   4-1. [适配器的初始化](#适配器的初始化)
   
   4-2.  [适配器的接口](#适配器的接口)
   
   4-3. [适配器的实现](#适配器的实现)

5. [拦截器](#拦截器)
   
   5-1. [拦截器接口](#拦截器接口)
   
   5-2. [拦截器的实现](#拦截器的实现)

6. [事件](#事件)
   
   6-1. [事件的定义](#事件的定义)
   
   6-2. [事件的使用](#事件的使用)

## 核心目录结构

1. adapters - 各平台的适配器
2. interceptor - 拦截器
3. pluginjs - 三方插件
4. utils - 工具函数
5. nodescript - Cocos节点脚本

## 主要文件

1. AdSdk.ts - 广告管理核心类
2. AdConfig.ts - 广告配置
3. Types.ts - 类型定义

## AdSdk

`AdSdk` 是广告管理的核心类，实现了 `AdInterface` 接口，提供了广告的基本操作，
如：初始化、加载、展示、隐藏等，但具体的广告操作需要由**适配器**实现。

### 初始化流程
1. 调用 `AdSdk.instance.init` 方法初始化
2. `AdSdk` 会根据平台名称，加载对应的拦截器、适配器以及广告配置 `AdConfig`
3. 适配器加载完成后，会调用适配器的 `init` 方法初始化广告
4. `init` 方法将依次触发加载的拦截器 `LoginInterceptor`、`RemoteConfigInterceptor`、`AdStrategyInterceptor`
5. `LoginInterceptor` 拦截器会检查用户是否登录，如果未登录，则会触发隐私登录，调用适配器的登录方法（**每个适配器需要实现自己的登录逻辑**)
6. `RemoteConfigInterceptor` 拦截器会根据平台登录返回的信息，请求后端登录服务，返回配置信息并解析广告数据
7. `AdStrategyInterceptor` 拦截器根据后端广告配置数据，执行广告展示策略，如自动播放广告等，同时负责接收广告事件并上报后端服务
8. 拦截器执行完成后，适配器也就初始化完成，`AdSdk` 将触发初始化完成事件 `AdEventType.SdkInited`，初始化结束。

### 配置适配器

在`AdSdk.ts`文件中，配置适配器与平台的映射关系。

```js
const adapters: Record<string, () => Promise<any>> = {
  [Platform.WEB]: () => import('./adapters/web/WebAd'),
  [Platform.ALIPAY]: () => import('./adapters/alipay/AlipayAd'),
  [Platform.BOX4399]: () => import('./adapters/box4399/Box4399Ad'),
  [Platform.HUAWEI]: () => import('./adapters/huawei/HuaweiAd'),
  [Platform.KS]: () => import('./adapters/ks/KsAd'),
  [Platform.M4399]: () => import('./adapters/m4399/M4399Ad'),
  [Platform.OPPO]: () => import('./adapters/oppo/OppoAd'),
  [Platform.TT]: () => import('./adapters/tt/TtAd'),
  [Platform.VIVO]: () => import('./adapters/vivo/VivoAd'),
}
```

### 适配器的加载

在`AdSdk.ts`初始化中，设置平台时会自动加载对应的适配器，并初始化。

```js
private setPlatform(platform: string, config?: AdInitConfig): Promise<void> {
  this._platform = platform
  this._config = config ?? this._config ?? {}
  this.configInterceptors(this._platform)
  return this.loadAdapter(this._platform)
}
```

在 `loadAdapter` 方法中，根据平台获取对应的适配器，可在此方法内配置默认拦截器。
注意在 `getConfig` 方法中，可根据平台获取对应的配置，根据平台名称如：Platform.TT，去 `AdConfig.ts` 中获取配置。

```js
private async loadAdapter(name: string): Promise<AdInterface>  {
    const module = await adapters[name]()
    const config = this.getConfig(name)
    this._config.adConfig = config
    const adapter = new module.default(this._config)
    if (adapter) {
      AdSdk.log(`适配器 [${name}] 加载完成`, config)
      this._adapter = adapter
      await this.invoke('init', this._config) ?? Promise.resolve()
      AdSdk.log(`适配器 [${this._platform}] 加载完成`)
      return Promise.resolve()
    }
    return Promise.reject('适配器加载失败')
}
```

## 适配器

在 `adapters` 目录下，实现对应的适配器，按平台命名。
每个适配器目录下，保证有平台对应文件如 `HuaweiAd.ts` 文件，该文件导出适配器的实现类，方便调用可创建对应的 `HwLog.ts` 文件。
只要确保 `HuaweiAd.ts` 文件导出的类实现了 `AdInterface` 接口即可，具体类型的广告实现可自行处理。

**建议：** 所有类型广告都实现为一个类，如：`BannerAd.ts`、`IntersAd.ts`、`RewardAd.ts`、`NativeAd.ts`、`CustomAd.ts`，并且有一个基类 `BaseAd.ts`，方便统一管理，做事件触发，否则必须保证实现的广告类有触发对应的事件。

### 适配器的初始化

适配器构造函数和 `init` 方法都会接收 `AdInitConfig` 类型的参数，区别在于，当适配器初始化时，会先执行拦截器的 `init` 方法，然后再执行适配器的 `init` 方法。如果拦截器修改了 `AdInitConfig` 参数，则 `init` 收到的是拦截器修改后的参数。

请保证在 `init` 方法初始化广告，因为这时的`AdInitConfig` 参数包含平台的配置信息

### 适配器的实现

适配器可实现  AdInterface, IPrivacyLogin 接口。
1. `AdInterface` 接口是广告的基本接口，实现该接口即可使用 `AdSdk` 提供的广告方法。
2. `IPrivacyLogin` 接口是隐私登录的接口，实现该接口即可触发隐私登录方法。

### 适配器的接口

在 `Types.ts` 中，定义了广告的接口 `AdInterface`，适配器需要实现该接口。
调用 `AdSdk` 方法都会转发到适配器的对应方法中。

```js
interface AdInterface {
  init(config?: AdInitConfig): any

  showBox(param?: AdParam): Promise<AdInvokeResult>
  showBanner(param?: AdParam): Promise<AdInvokeResult>
  hideBanner(param?: AdParam): Promise<AdInvokeResult>
  showInters(param?: AdParam): Promise<AdInvokeResult>
  showReward(param?: AdParam): Promise<AdInvokeResult>
  showNative(param?: AdParam): Promise<AdInvokeResult>
  showCustom(param?: AdParam): Promise<AdInvokeResult>
  hideCustom(param?: AdParam): Promise<AdInvokeResult>
  showToast(msg: string, duration: number): void
}
```

## 拦截器

在 `interceptors` 目录下，可以实现自定义的拦截器，拦截器的实现需要实现 `AdInterceptor` 接口。
拦截器必须在 `index.ts` 文件中导出。

将拦截器添加到适配器中，只要实现了对应的拦截方法，当适配器调用对应的方法时，会先执行拦截器的方法。

### 拦截器接口

拦截器接口跟适配器的接口类似，只不过拦截器的多了一个 `attach` 方法，用于初始化。

```js
interface AdInterceptor {
  attach(): void
  init?: (next: AdInitNext , param?: AdInitConfig) => any
  showBox?: (next: AdInvokeNext, param?: AdParam) => Promise<AdInvokeResult> | void
  showBanner?: (next: AdInvokeNext, param?: AdParam) => Promise<AdInvokeResult> | void
  hideBanner?: (next: AdInvokeNext, param?: AdParam) => Promise<AdInvokeResult> | void
  showInters?: (next: AdInvokeNext, param?: AdParam) => Promise<AdInvokeResult> | void
  showReward?: (next: AdInvokeNext, param?: AdParam) => Promise<AdInvokeResult> | void
  showNative?: (next: AdInvokeNext, param?: AdParam) => Promise<AdInvokeResult> | void
  showCustom?: (next: AdInvokeNext, param?: AdParam) => Promise<AdInvokeResult> | void
  hideCustom?: (next: AdInvokeNext, param?: AdParam) => Promise<AdInvokeResult> | void
}
```

### 拦截器的实现

在 `attach` 方法中，可以实现一些初始化的逻辑，如：监听广告事件，上报到后端等。

1. 参数 `next` 是下一个拦截器或适配器的方法，需要调用 `next` 方法才能继续执行。
2. 参数 `param` 是适配器的参数，可在拦截器中修改参数，然后传递给下一个拦截器或适配器。
3. 拦截器的方法返回非 `Promise` 对象时，表示中止后续的拦截器或适配器的执行。

```js
export class TTInterceptor implements AdInterceptor {
    private startTime: number
    private rule1: boolean = true
    private lastShowAt: number = 0
    public attach() {
      this.startTime = Date.now()
    }

    showInters (next: AdInvokeNext, param: AdParam): Promise<AdInvokeResult>| void {
      if (this.rule1) {
        this.rule1 = (Date.now() - this.startTime) < 30000
        if (this.rule1) return
      }
      let passTime = Date.now() - this.lastShowAt
      if (passTime < 60000) return

      return next(param).then(res => {
        this.lastShowAt = passTime  // 更新展示时间
        return res
      })
    }

    showReward (next: AdInvokeNext, param: AdParam): Promise<AdInvokeResult>| void {
      return next(param).then(res => {
        this.lastShowAt = Date.now()  // 更新展示时间
        return res
      })
    }
}
```

## 事件

在适配器具体广告实现中，为了减少非广告代码对广告逻辑的影响，可使用事件机制，如：`AdEventBus`。

在拦截器或其他地址监听指定事件，做相关逻辑处理，如上报信息等。

### 事件的定义

在 `Types.ts` 中，定义了事件的类型，如：`AdEventType`，可在 `AdEventBus` 中使用。

```js
enum AdEventType {
  AdLoaded = 'ad:loaded',
  AdClosed = 'ad:closed',
  AdError = 'ad:error',
  AdReward = 'ad:reward',
  AdShowed = 'ad:showed',
  AdHided = 'ad:hided',
  AdClicked = 'ad:clicked',
}
```

### 事件的使用

在适配器具体广告实现中，使用 `AdEventBus` 触发事件，如：`AdEventBus.instance.emit(AdEventType.AdLoaded)`。

```js
 AdEventBus.instance.on(AdEventType.AdLoaded, (ad: AdBase, ...otherArgs) => {
    // do something
 })

 AdEventBus.instance.emit(AdEventType.AdLoaded, ad, ...otherArgs);
```
